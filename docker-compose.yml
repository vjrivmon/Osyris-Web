version: '3.8'

services:
  # Servicio de frontend (Next.js)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: osyris-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://backend:5000
    depends_on:
      - backend
    networks:
      - osyris-network

  # Servicio de backend (Express.js)
  backend:
    build:
      context: ./api-osyris
      dockerfile: Dockerfile
    container_name: osyris-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=osyris_user
      - DB_PASSWORD=osyris_password
      - DB_NAME=osyris_db
      - JWT_SECRET=osyrisScoutGroup2024SecretKey
      - JWT_EXPIRES_IN=24h
    depends_on:
      - db
    networks:
      - osyris-network

  # Servicio de base de datos (PostgreSQL)
  db:
    image: postgres:15-alpine
    container_name: osyris-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=osyris_user
      - POSTGRES_PASSWORD=osyris_password
      - POSTGRES_DB=osyris_db
    volumes:
      - osyris-db-data:/var/lib/postgresql/data
    networks:
      - osyris-network

networks:
  osyris-network:
    driver: bridge

volumes:
  osyris-db-data:
    driver: local 